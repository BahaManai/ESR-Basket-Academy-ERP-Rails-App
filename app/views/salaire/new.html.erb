<% content_for :title, "New salaire" %>

<h1>New salaire</h1>

<%= form_with(model: @salaire) do |form| %>
  <% if @salaire.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(@salaire.errors.count, "error") %> prohibited this salaire from being saved:</h2>

      <ul>
        <% @salaire.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div>
    <%= form.label :entraineur_id, style: "display: block" %>
    <%= form.select :entraineur_id, Entraineur.pluck(:nom, :id), prompt: "Choisir un entraîneur" %>
  </div><br>

  <div>
    <%= form.label :salaire, style: "display: block" %>
    <%= form.text_field :salaire %>
  </div>

  <div>
    <%= form.label :mois, style: "display: block" %>
    <%= form.number_field :mois %>
  </div>

  <div>
    <%= form.label :annee, style: "display: block" %>
    <%= form.number_field :annee %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>

<br>

<div>
  <%= link_to "Back to salaires", salaires_path %>
</div>
