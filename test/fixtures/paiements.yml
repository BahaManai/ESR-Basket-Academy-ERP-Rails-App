# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

one:
  montant: 1.5
  date_abonnement: 2025-01-03 # Valid date, but should be <= today's date for testing
  date_encaissement: 2025-01-03 # Valid date, but should be <= today's date for testing
  etat_abonnement: "Non crédit" # Valid value (either "Non crédit" or "Crédit")
  num_recu: 12345 # Example valid num_recu (numeric value >= 0)
  joueur: one

two:
  montant: 1.5
  date_abonnement: 2025-01-03 # Valid date, but should be <= today's date for testing
  date_encaissement: 2025-01-03 # Valid date, but should be <= today's date for testing
  etat_abonnement: "Crédit" # Valid value (either "Non crédit" or "Crédit")
  num_recu: 67890 # Example valid num_recu (numeric value >= 0)
  joueur: two
